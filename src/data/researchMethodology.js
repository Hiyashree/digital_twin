/**
 * Aligns portal UX with the stated research approach:
 * wastage-type classification + tourist hotspot mapping (Meghalaya) via ViT.
 */

export const RESEARCH_PROBLEM = {
  title: "Research focus",
  summary:
    "Wastage type classification and hotspot area mapping in Meghalaya using deep learning / Vision Transformer — with field datasets and comparison to recent vision architectures.",
};

/**
 * Six baseline families + proposed ViT row (fill cells from your experiments).
 * Columns: waste-type task (multi-class) and recyclability task (binary).
 * Empty array → Benchmarks tab shows instructions only until you add measured rows.
 */
export const BENCHMARK_COMPARISON_ROWS = [];
